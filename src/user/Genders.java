package user;

/**
 * Enumerator for a user's gender. Can be set to MALE or FEMALE. This is not a political statement, we just wanted the opportunity to use an enum for something.
 */
public enum Genders {
	MALE, FEMALE
}
